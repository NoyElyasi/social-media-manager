/**
 * הצעת שירים (שם בלבד, לא קובץ אודיו) לפי סעיף 4.2/4.4 — מבוססת על נושא
 * (theme) הפוסט, ורשימת השירים לכל נושא ניתנת לעריכה מלאה בהגדרות (ראו
 * SongsByThemeForm) — ההצעה היא תמיד שיר שהמשתמשת עצמה אישרה כמתאים לנושא.
 *
 * הנושא עצמו לא חייב בחירה ידנית: אם לא נבחר נושא בזמן יצירת הפוסט (ראו
 * detectThemeFromText), מזהים אותו אוטומטית ממילות מפתח בטקסט, לפי אותו
 * אוצר מילים של נושאים שגם משמש לתיוג ידני/לרשימות השירים — לא ניחוש גס
 * ומנותק כמו ההיוריסטיקה המקורית (שהחזירה כל הזמן את אותם 1-2 שירים
 * קבועים), אלא זיהוי שמזין בדיוק את אותה מערכת "שיר לפי נושא" שכבר קיימת.
 * בחירה ידנית, כשיש, תמיד גוברת על הזיהוי האוטומטי.
 */

export interface SongSuggestion {
  title: string;
  artist: string;
}

/**
 * מילות מפתח לזיהוי אוטומטי של נושא מתוך טקסט הפוסט — מכוון לאוצר המילים
 * של אפשרויות הנושא הקבועות בברירת המחדל (ראו aiThemeOptions ב-schema).
 * נושא מותאם-אישית שהמשתמשת מוסיפה בהגדרות ולא מופיע כאן פשוט לא יזוהה
 * אוטומטית (עדיין אפשר לבחור אותו ידנית) — לא קורס, לא שגיאה.
 */
const THEME_KEYWORDS: Record<string, string[]> = {
  "פחד": ["פחד", "פוחד", "פוחדת", "מפחיד", "מפחידה", "חרדה", "חרד", "מבועת", "מבועתת", "איום", "מסוכן", "מסוכנת"],
  "קנאה": ["קנאה", "מקנא", "מקנאת", "קנאתי", "להתחרות", "תחרות", "השוואה", "משווה"],
  "אהבה": ["אהבה", "אוהב", "אוהבת", "אהוב", "אהובה", "התאהבתי", "התאהבנו", "זוגיות", "רומנטי", "רומנטית", "לב שלם"],
  "כעס": ["כעס", "כועס", "כועסת", "זעם", "זועם", "עצבים", "מתעצבנת", "מתעצבן", "רותח", "רותחת", "נסער", "נסערת"],
  "געגוע": ["געגוע", "געגועים", "מתגעגע", "מתגעגעת", "חסר לי", "חסרה לי", "מתגעגעים"],
  "בדידות": ["בודד", "בודדה", "בדידות", "לבד", "מבודד", "מבודדת", "ניכור"],
  "בושה": ["בושה", "מתבייש", "מתביישת", "נבוך", "נבוכה", "מבוכה", "מתביישים"],
  "אשמה": ["אשמה", "אשם", "אשמים", "מצפון", "רגשי אשם", "הרגשת אשם"],
  "שמחה": ["שמחה", "שמח", "שמחתי", "מאושר", "מאושרת", "חוגג", "חוגגת", "חוגגים", "שמחים"],
  "תסכול": ["תסכול", "מתוסכל", "מתוסכלת", "מיואש", "מיואשת", "נכשלתי", "נכשל", "נכשלה"],
  "נוסטלגיה": ["נוסטלגיה", "נוסטלגי", "פעם היה", "בעבר", "ילדות", "זיכרון", "זכרון", "נזכרתי", "נזכרת"],
  "ביקורת חברתית": ["פוליטיקה", "ממשלה", "מדינה", "חוק", "זכויות", "אי-צדק", "אי צדק", "מחדל", "עוול"],
};

/**
 * סורקת את הטקסט אחרי מילות המפתח של כל נושא זמין (מתוך availableThemes —
 * הרשימה שהמשתמשת קבעה בהגדרות, לא בהכרח כל הנושאים ב-THEME_KEYWORDS),
 * ומחזירה את הנושא עם הכי הרבה התאמות. שוויון/אין התאמה בכלל = null (לא
 * מנחשת סתם — עדיפה "אחר" נכון על ניחוש שגוי).
 */
export function detectThemeFromText(text: string, availableThemes: string[]): string | null {
  let bestTheme: string | null = null;
  let bestCount = 0;

  for (const theme of availableThemes) {
    const keywords = THEME_KEYWORDS[theme];
    if (!keywords) continue;
    const count = keywords.filter((kw) => text.includes(kw)).length;
    if (count > bestCount) {
      bestCount = count;
      bestTheme = theme;
    }
  }

  return bestTheme;
}

export type ThemeSongs = Record<string, SongSuggestion[]>;

const FALLBACK_THEME = "אחר";
const HARD_FALLBACK: SongSuggestion[] = [
  { title: "כאן ביחד", artist: "שרית חדד" },
  { title: "יום יבוא", artist: "עידן רייכל" },
];

/**
 * theme — הנושא שנבחר לפוסט (מתוך aiThemeOptions), או null אם לא נבחר.
 * themeSongs — המפה הנוכחית (מוגדרות/הגדרות), theme → רשימת שירים.
 * אם לנושא הנבחר אין שירים שמורים (רשימה ריקה/לא קיימת), חוזרים לדלי "אחר";
 * ואם גם זה ריק, לרשימת ברירת המחדל הקשיחה כאן (כך שההצעה לא נעלמת לגמרי).
 */
export function suggestSongs(theme: string | null | undefined, themeSongs: ThemeSongs, max = 3): SongSuggestion[] {
  const forTheme = theme ? themeSongs[theme] : undefined;
  const songs = forTheme && forTheme.length > 0 ? forTheme : themeSongs[FALLBACK_THEME];
  const result = songs && songs.length > 0 ? songs : HARD_FALLBACK;
  return result.slice(0, max);
}
